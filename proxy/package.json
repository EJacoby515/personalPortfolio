{
  "name": "ejacoby-chat-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the portfolio chatbot to OpenRouter, keeping the API key server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put OPENROUTER_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
